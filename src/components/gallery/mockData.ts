
import { GalleryImage, GalleryVideo } from "./types";

// Empty arrays instead of sample data
export const galleryImages: GalleryImage[] = [];

// Empty array for video data
export const galleryVideos: GalleryVideo[] = [];
